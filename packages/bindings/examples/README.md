# Bindings Vite Examples

该目录提供 bindings 的 Vite 示例入口，用于验证 `canvaskit_cheap.wasm` 与主要 API（Surface/Canvas/WebGL/WebGPU/Paragraph/Filters）。

## 开发运行

在 packages/bindings 目录：

- `pnpm dev:examples`

启动后访问 Vite 输出的本地地址，查看示例页面与日志。
